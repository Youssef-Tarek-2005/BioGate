# Successfully installed packages
PyQt5==5.15.11
requests==2.32.5
paho-mqtt==2.1.0
cryptography
matplotlib==3.10.8
pytest==9.0.2
coverage==7.13.1
sqlalchemy==2.0.45
pyinstaller==6.17.0
PyOpenGL==3.1.10
PyOpenGL_accelerate==3.1.10
pandas==2.3.3
scikit-learn==1.8.0

# Packages with Python 3.14 compatibility issues:
# - numpy (requires GCC >= 8.4)
# - tensorflow (no compatible version)
# - opencv-python (depends on numpy)
# - dlib (compilation issues)
# - face-recognition (depends on dlib)
# - pyqt5-tools (qmake dependency)
# - vtk (no compatible version)
# - pymongo (may have issues)
